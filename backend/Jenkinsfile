pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/LeonardoTonon1234/catalogo-filmes-aws-Final.git'
            }
        }

        stage('Instalar JDK') {
            steps {
                echo 'Instalando OpenJDK 17...'
                sh '''
                    apt-get update -y
                    apt-get install -y openjdk-17-jdk
                    java -version
                '''
            }
        }

        stage('Build') {
            steps {
                dir('backend') {
                    echo 'Executando build do projeto...'
                    sh './gradlew clean build -x test'
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                dir('backend') {
                    echo 'Construindo imagem Docker...'
                    sh 'docker build -t catalogo-filmes .'
                }
            }
        }

        stage('Run Container') {
            steps {
                echo 'Executando container do catálogo de filmes...'
                sh 'docker run -d -p 8082:8080 --name catalogo-filmes catalogo-filmes || true'
            }
        }
    }
}
